[global]
#required so it will listen for external connections
server.socket_host = "0.0.0.0"
server.socket_port = 8081
server.thread_pool = 30

tools.encode.on = True
tools.decode.on = True
tools.trailing_slash.on = True

log.screen = True

database = "bowerbird-proxy.db"
root_dir = '/var/lib/bowerbird-proxy'
extension = '.wv'
localnet_scan_time_ms = 500
# note, this is the number of processes doing transfers so too many may
# slow down your computer.
concurrent_transfers = 1
transfer_bandwidth = 1048576
transfer_chunk_size = 1048576
